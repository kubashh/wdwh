export class AST {
  source;
  i = 0;

  constructor(source: string) {
    this.source = source;
  }

  generate() {
    // const node: ASTNode = {
    //   type: `program`,
    //   props: {},
    //   children: [],
    // };
    // return node;
  }

  next() {}
}
